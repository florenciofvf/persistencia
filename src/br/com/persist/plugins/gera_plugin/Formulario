package ###package###;

import java.awt.BorderLayout;
import java.awt.Window;

import br.com.persist.abstrato.AbstratoFormulario;
import br.com.persist.formulario.Formulario;

public class ###nameCap###Formulario extends AbstratoFormulario {
	private static final long serialVersionUID = 1L;
	private final ###nameCap###Container container;

	private ###nameCap###Formulario(Formulario formulario) {
		super(formulario, ###nameCap###Mensagens.getString(###nameCap###Constantes.LABEL_###nameUpper###));
		container = new ###nameCap###Container(this, formulario);
		container.set###nameCap###Formulario(this);
		montarLayout();
	}

	private ###nameCap###Formulario(###nameCap###Container container) {
		super(container.getFormulario(), ###nameCap###Mensagens.getString(###nameCap###Constantes.LABEL_###nameUpper###));
		container.set###nameCap###Formulario(this);
		this.container = container;
		container.setJanela(this);
		montarLayout();
	}

	private void montarLayout() {
		add(BorderLayout.CENTER, container);
	}

	public static void criar(Formulario formulario, ###nameCap###Container container) {
		###nameCap###Formulario form = new ###nameCap###Formulario(container);
		Formulario.posicionarJanela(formulario, form);
	}

	public static void criar(Formulario formulario) {
		###nameCap###Formulario form = new ###nameCap###Formulario(formulario);
		Formulario.posicionarJanela(formulario, form);
	}

	public void excluirContainer() {
		remove(container);
		container.setJanela(null);
		container.set###nameCap###Formulario(null);
		fechar();
	}

	@Override
	public void windowOpenedHandler(Window window) {
		container.windowOpenedHandler(window);
	}
}