<set>
    <![CDATA[
        senao(igual($DESCRICAO, SEG), SEGUNDA-FEIRA,
               senao(igual($DESCRICAO, TER), TERCA-FEIRA,
                      senao(igual($DESCRICAO, QUA), QUARTA-FEIRA,
                             senao(igual($DESCRICAO, QUI), QUINTA-FEIRA,
                                    senao(igual($DESCRICAO, SEX), SEXTA-FEIRA,
                                           senao(
                                                 ou(igual($DESCRICAO, SAB),igual($DESCRICAO, DOM)), 
                                                 FINAL DE SEMANA, 'DIA INVÁLIDO'
                                           )
                                    )
                             )
                      )
               )
        )
    ]]>
</set>
<set>
    <![CDATA[
        switch(
              igual($DESCRICAO, SEG), SEGUNDA-FEIRA,
              igual($DESCRICAO, TER), TERCA-FEIRA,
              igual($DESCRICAO, QUA), QUARTA-FEIRA,
              igual($DESCRICAO, QUI), QUINTA-FEIRA,
              igual($DESCRICAO, SEX), SEXTA-FEIRA,
              ou(igual($DESCRICAO, SAB),igual($DESCRICAO, DOM)), FINAL DE SEMANA, 
              'DIA INVÁLIDO'
        )
    ]]>
</set>
<set>
    <![CDATA[
        isEmpty($DATA_INICIADA)
    ]]>
</set>
<set>
    <![CDATA[
        $DATA_INICIADA
    ]]>
</set>
<set>
    <![CDATA[
        returnNull(
            set(connEleicao,
                getConnection('com.mysql.cj.jdbc.Driver','jdbc:mysql://localhost/eleicao','root','***')
            )
        )
    ]]>
</set>
<set>
    <![CDATA[
        returnNull(
            set(listaIdCandidato,
                select('connEleicao', 'select id from candidato2 where eleicao_id = idEleicao', idEleicao, $ID)
            )
        )
    ]]>
</set>
<set>
    <![CDATA[
        ifElse(isEmpty($listaIdCandidato), SEM CANDIDATO2, CONTÉM CANDIDATO2)
    ]]>
</set>