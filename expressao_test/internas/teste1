package br.com.teste;

defun main() {
	const interna = getInterna(2);
	return interna(2);
}

defun getInterna(valor) {
	return defun somar(x) {
		return valor + x;
	};
}

defun teste(funcao) {
	return defun inner(param) {
		return defun outra() {
			return funcao();
		};
	};
}

/*defun fatorial(numero) {
	const exec = defun calcular(valor) {
					if(valor == 0) {
						return 1;
					} else {
						return valor * calcular(valor - 1);
					}
				};
	return exec(numero);
}*/